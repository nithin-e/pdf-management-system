A full-stack web application that allows users to upload a PDF file, preview all pages, select specific pages, and generate a new PDF containing only the chosen pages.

This project is built as part of a Fullstack Developer Task, using React (Frontend) and Node.js + Express (Backend).
